/**
 * Wireframe org data — dummy roster (see dummyDataset.ts).
 * Client MasterList JSON is no longer loaded in the UI.
 */
export {
  departments,
  designations,
  employees,
  sections,
  employeeHistory,
  getDepartment,
  getDesignation,
  getEmployee,
  getSection,
  getEmployeeHistory,
  getDirectReports,
} from './dummyDataset'

export { workbookSheetNames } from './workbookSheets'
